[pytest]
testpaths = tests
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
markers =
    browser: needs Playwright's bundled Chromium (skipped automatically if unavailable)
    network: needs internet access / real yt-dlp calls (deselect with -m "not network")
addopts = -ra
